# Force LF line endings for shell scripts regardless of local git config
# (Windows commonly auto-converts to CRLF on checkout, which breaks the
# #!/bin/bash shebang line on Linux - see notes/05-environments-dev.txt
# for the real incident this caused).
*.sh text eol=lf
